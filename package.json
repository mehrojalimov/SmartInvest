{
  "scripts": {
    "start": "cd app && node server.js",
    "setup": "psql -U postgres -f setup.sql",
    "api": "cd app/services && node yahooFinance.js",
    "start:local": "env-cmd node app/server.js",
    "setup:local": "env-cmd psql -d postgres -c 'create database smartinvest' -f setup.sql"

  },
  "dependencies": {
    "argon2": "^0.41.1",
    "axios": "^1.7.7",
    "cookie-parser": "^1.4.7",
    "express": "^4.21.1",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.5.9"
  }
}
